# FreshRSS Configuration
FRESHRSS_URL=http://freshrss:80
FRESHRSS_USER=admin
FRESHRSS_API_PASSWORD=

# Site URL (CRITICAL for better-auth — must match externally-accessible origin)
PUBLIC_SITE_URL=http://localhost:4321

# Email (Mailpit in local dev)
SMTP_HOST=mailpit
SMTP_PORT=1025
SMTP_FROM=noreply@localhost

# S3/R2 (MinIO in local dev)
S3_ENDPOINT=http://minio:9000
S3_ACCESS_KEY=localadmin
S3_SECRET_KEY=localpassword
S3_BUCKET=community-images

# Media URL (for rewritten image URLs)
MEDIA_BASE_URL=http://localhost:9000/community-images

# Cloudflare Zero Trust (not needed locally)
# CF_ACCESS_CLIENT_ID=
# CF_ACCESS_CLIENT_SECRET=
